# Prompt: Register a New Project

This is a **new project** governed by `pri8771/iOS_app_factory_rules`.

Before broad implementation:

1. Add the factory project-registration structure without deleting existing files.
2. Set `.factory/project-context.json` to `projectType: "new"`.
3. Complete the platforms, lifecycle status, constraints, persistence, backend, privacy, and build commands from available evidence.
4. Create or update root `AGENTS.md` so all future agents read the project context and quality manifest first.
5. Establish app-specific architecture, features, decisions, risks, assumptions, test plan, and quality manifest.
6. Create feature contracts for the first one to three meaningful user outcomes.
7. Implement one real vertical feature slice before creating broad decorative UI.
8. Report what was verified and what remains unverified. Do not call the project done based only on scaffolding or compilation.
