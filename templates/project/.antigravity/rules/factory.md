# App Factory Antigravity Adapter

Use `AGENTS.md` as the portable operating contract. Before execution, read `.factory/current-state.yaml`, the active task, and the latest handoff. Questions and proposals remain read-only. Scheduled work must operate only on enrolled repositories, use isolated worktrees for concurrent writers, run deterministic verification, and leave a checkpoint.
