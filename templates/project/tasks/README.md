# Portable Tasks

Execution work is stored as YAML task records and moves through `backlog`, `active`, `blocked`, `review`, and `done`.

Advisory questions and read-only recommendations do not require tasks. Scope-changing proposals remain proposals until explicitly approved for execution.
